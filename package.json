{
  "name": "sprintstack",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --cwd apps/api dev",
    "test": "bun test --cwd apps/api",
    "typecheck": "bun run --cwd apps/api typecheck"
  }
}
